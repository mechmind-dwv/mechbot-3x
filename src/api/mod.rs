// Módulo de API - Por implementar
pub struct ApiServer;

impl ApiServer {
    pub fn new() -> Self {
        Self
    }
    
    pub fn start(&self) -> Result<(), anyhow::Error> {
        Ok(())
    }
}
