// Módulo de navegación - Por implementar
pub struct NavigationController;

impl NavigationController {
    pub fn new() -> Self {
        Self
    }
    
    pub fn plan_path(&self) -> Result<(), anyhow::Error> {
        Ok(())
    }
}
