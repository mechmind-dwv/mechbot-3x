use crate::config::Config;
use anyhow::Result;

pub struct Robot {
    config: Config,
}

impl Robot {
    pub async fn new(config: Config) -> Result<Self> {
        Ok(Robot { config })
    }
    
    pub async fn start(&self) -> Result<()> {
        println!("🤖 Robot systems initialized");
        Ok(())
    }
}
